# SPDX-FileCopyrightText: 2026 Benoit Rolandeau <borlnov.obsessio@gmail.com>
#
# SPDX-License-Identifier: Apache-2.0

name: brorum
description: |-
  BROrum is a local network voting and deliberation app that enables secure, offline-first
  collective decision-making.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.1.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # Internationalize Flutter apps
  flutter_localizations:
    sdk: flutter

  flutter_bloc: ^9.1.1

  # Adapting screen and font size
  flutter_screenutil: ^5.9.3

  # Code to deal with internationalized/localized messages
  intl: ^0.20.2

  act_config_manager:
    path: actlibs/act_config_manager

  act_dart_utility:
    path: actlibs/act_dart_utility

  act_flutter_utility:
    path: actlibs/act_flutter_utility

  act_global_manager:
    path: actlibs/act_global_manager

  act_intl:
    path: actlibs/act_intl

  act_intl_ui:
    path: actlibs/act_intl_ui

  act_licenses_manager:
    path: actlibs/act_licenses_manager

  act_life_cycle:
    path: actlibs/act_life_cycle

  act_local_storage_manager:
    path: actlibs/act_local_storage_manager

  act_logger_manager:
    path: actlibs/act_logger_manager

  act_platform_manager:
    path: actlibs/act_platform_manager

  act_router_manager:
    path: actlibs/act_router_manager

  act_themes_manager:
    path: actlibs/act_themes_manager

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Used to generate the translation files
  intl_utils: ^2.8.10

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Fonts
  fonts:
    - family: BBBReadMe
      fonts:
        - asset: fonts/BBBReadMe/static/BBBReadMe-Regular.ttf
          weight: 400
        - asset: fonts/BBBReadMe/static/BBBReadMe-Italic.ttf
          weight: 400
          style: italic
        - asset: fonts/BBBReadMe/static/BBBReadMe-SemiBold.ttf
          weight: 600
        - asset: fonts/BBBReadMe/static/BBBReadMe-SemiBoldItalic.ttf
          weight: 600
          style: italic
        - asset: fonts/BBBReadMe/static/BBBReadMe-Bold.ttf
          weight: 700
        - asset: fonts/BBBReadMe/static/BBBReadMe-BoldItalic.ttf
          weight: 700
          style: italic
        - asset: fonts/BBBReadMe/static/BBBReadMe-Black.ttf
          weight: 900
        - asset: fonts/BBBReadMe/static/BBBReadMe-BlackItalic.ttf
          weight: 900
          style: italic

  # Project assets
  assets:
    - assets/
    - assets/config/
    - assets/graphics/

    # The licenses are included as assets to be displayed in the licenses page of the application.
    - LICENSES/
    - actlibs/LICENSES/

flutter_intl:
  enabled: true
  # Sets the name for the generated localization class. Default: S
  class_name: Tr
  # Sets the main locale used for generating localization files.
  main_locale: en_GB
